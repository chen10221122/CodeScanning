import { Container as Com } from '@/pages/area/areaF9/style';

export const Container = ({ children }: { children: any }) => {
  /* @ts-ignore */
  return <Com padding={'16px'} children={children} />;
};
